# Design System

## Experience

物理场景：技术美术在双显示器工作站中长时间对照 Maya、资源管理器和交付清单，环境光偏暗，需要快速发现异常但不能让警告色淹没内容。

视觉母题为“资产审阅台”：左侧是有顺序的工作阶段，中部是可筛选的文件账本，右侧是贴近当前选择的证据检查器。密度中高，平面分区清晰，不使用卡片墙。

## Color

设计源 token 使用 OKLCH；Qt 样式表使用同色板的 sRGB 映射值。

- `bg`: `oklch(0.145 0.008 160)`，应用背景；
- `surface`: `oklch(0.205 0.012 160)`，侧栏与工具栏；
- `surface-raised`: `oklch(0.255 0.016 160)`，选中与检查器；
- `ink`: `oklch(0.94 0.008 160)`，主要文字；
- `muted`: `oklch(0.72 0.018 160)`，次要文字；
- `primary`: `oklch(0.55 0.119 160)`，扫描、选中与已验证；
- `accent`: `oklch(0.78 0.145 72)`，提醒与待处理；
- `danger`: `oklch(0.64 0.19 28)`，阻断与错误；
- `info`: `oklch(0.69 0.115 240)`，信息状态。

任何状态同时使用图标或文字。饱和色填充使用近白文字。

## Typography

使用 Windows 中文系统字体栈 `Microsoft YaHei UI, Segoe UI, sans-serif`。路径、哈希、版本号和规则 ID 使用 `Cascadia Mono, Consolas, monospace`。标题 20/16 px，正文 13 px，辅助信息 12 px；不使用全大写英文作为导航层级。

## Layout

- 默认窗口 1440×900，最小窗口 1080×680；
- 顶栏承载产品、当前只读状态和全局操作；
- 左栏 220 px，展示六个真实工作阶段；
- 中央区域随窗口伸缩；文件与问题表格保持主视觉面积；
- 右侧检查器约 320 px，仅展示当前选择的元数据和问题证据；
- 窄窗口隐藏右侧检查器，由“查看详情”动作重新展开。

## Components

- 主按钮使用深苔绿色实底，标签采用动词加对象；
- 次按钮保持平面描边，危险动作只使用语义红；
- 路径选择器必须同时支持对话框与可复制文本；
- 表格选中行用低饱和绿色表面，不使用强辉光；
- 状态摘要采用紧凑分段条，阻断、错误、警告、通过有文字和数量；
- 空态说明下一步，错误态说明哪些内容没有改变以及如何恢复；
- 扫描期间禁用会改变上下文的控件，完成后恢复。

## Motion

高频选择即时响应。页面切换与检查器更新采用 150–200 ms 淡入；扫描进度使用确定或不确定进度条。遵循系统减少动态效果设置，不以动画隐藏默认内容。
